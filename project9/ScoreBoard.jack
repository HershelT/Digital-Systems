class ScoreBoard {
    function void printCookies(int cookies) {
        var int memAddress;
        //Let memory address be row 0, collumn 28
        let memAddress = 16384 + (160 + 1)
        do Memory.poke(memAddress, cookies);
        return;
    }
    
}